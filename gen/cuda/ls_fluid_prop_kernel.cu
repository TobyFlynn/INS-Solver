//
// auto-generated by op2.py
//

//user function
__device__ void ls_fluid_prop_gpu( const double *step, double *nu, double *rho) {
  for(int i = 0; i < 10; i++) {
    nu[i] = 0.5 * nu0_cuda * (1.0 + step[i]) + 0.5 * nu1_cuda * (1.0 - step[i]);
    rho[i] = 0.5 * rho0_cuda * (1.0 + step[i]) + 0.5 * rho1_cuda * (1.0 - step[i]);
  }

}

// CUDA kernel function
__global__ void op_cuda_ls_fluid_prop(
  const double *__restrict arg0,
  double *arg1,
  double *arg2,
  int   set_size ) {


  //process set elements
  for ( int n=threadIdx.x+blockIdx.x*blockDim.x; n<set_size; n+=blockDim.x*gridDim.x ){

    //user-supplied kernel call
    ls_fluid_prop_gpu(arg0+n*10,
                  arg1+n*10,
                  arg2+n*10);
  }
}


//host stub function
void op_par_loop_ls_fluid_prop(char const *name, op_set set,
  op_arg arg0,
  op_arg arg1,
  op_arg arg2){

  int nargs = 3;
  op_arg args[3];

  args[0] = arg0;
  args[1] = arg1;
  args[2] = arg2;

  // initialise timers
  double cpu_t1, cpu_t2, wall_t1, wall_t2;
  op_timing_realloc(36);
  op_timers_core(&cpu_t1, &wall_t1);
  OP_kernels[36].name      = name;
  OP_kernels[36].count    += 1;


  if (OP_diags>2) {
    printf(" kernel routine w/o indirection:  ls_fluid_prop");
  }

  int set_size = op_mpi_halo_exchanges_grouped(set, nargs, args, 2);
  if (set_size > 0) {

    //set CUDA execution parameters
    #ifdef OP_BLOCK_SIZE_36
      int nthread = OP_BLOCK_SIZE_36;
    #else
      int nthread = OP_block_size;
    #endif

    int nblocks = 200;

    op_cuda_ls_fluid_prop<<<nblocks,nthread>>>(
      (double *) arg0.data_d,
      (double *) arg1.data_d,
      (double *) arg2.data_d,
      set->size );
  }
  op_mpi_set_dirtybit_cuda(nargs, args);
  cutilSafeCall(cudaDeviceSynchronize());
  //update kernel record
  op_timers_core(&cpu_t2, &wall_t2);
  OP_kernels[36].time     += wall_t2 - wall_t1;
  OP_kernels[36].transfer += (float)set->size * arg0.size;
  OP_kernels[36].transfer += (float)set->size * arg1.size * 2.0f;
  OP_kernels[36].transfer += (float)set->size * arg2.size * 2.0f;
}
