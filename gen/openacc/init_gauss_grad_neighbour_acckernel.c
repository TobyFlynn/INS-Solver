//
// auto-generated by op2.py
//

//user function
//user function
//#pragma acc routine
inline void init_gauss_grad_neighbour_openacc( const int *reverse, double *rx,
                            double *sx, double *ry,  double *sy,
                            double *Dx0, double *Dy0, double *Dx1, double *Dy1,
                            double *Dx2, double *Dy2) {

  double J[18];
  for(int i = 0; i < 18; i++) {
    J[i] = -sx[i] * ry[i] + rx[i] * sy[i];
  }

  for(int i = 0; i < 18; i++) {
    double rx_n = sy[i] / J[i];
    double sx_n = -ry[i] / J[i];
    double ry_n = -sx[i] / J[i];
    double sy_n = rx[i] / J[i];
    rx[i] = rx_n;
    sx[i] = sx_n;
    ry[i] = ry_n;
    sy[i] = sy_n;
  }

  if(reverse[0]) {
    for(int j = 0; j < 10; j++) {
      for(int i = 0; i < 6; i++) {
        int ind = j * 6 + i;
        Dx0[ind] = rx[i] * gF0DrR_g[ind] + sx[i] * gF0DsR_g[ind];
        Dy0[ind] = ry[i] * gF0DrR_g[ind] + sy[i] * gF0DsR_g[ind];
      }
    }
  } else {
    for(int j = 0; j < 10; j++) {
      for(int i = 0; i < 6; i++) {
        int ind = j * 6 + i;
        Dx0[ind] = rx[i] * gF0Dr_g[ind] + sx[i] * gF0Ds_g[ind];
        Dy0[ind] = ry[i] * gF0Dr_g[ind] + sy[i] * gF0Ds_g[ind];
      }
    }
  }

  if(reverse[1]) {
    for(int j = 0; j < 10; j++) {
      for(int i = 0; i < 6; i++) {
        int ind = j * 6 + i;
        Dx1[ind] = rx[i + 6] * gF1DrR_g[ind] + sx[i + 6] * gF1DsR_g[ind];
        Dy1[ind] = ry[i + 6] * gF1DrR_g[ind] + sy[i + 6] * gF1DsR_g[ind];
      }
    }
  } else {
    for(int j = 0; j < 10; j++) {
      for(int i = 0; i < 6; i++) {
        int ind = j * 6 + i;
        Dx1[ind] = rx[i + 6] * gF1Dr_g[ind] + sx[i + 6] * gF1Ds_g[ind];
        Dy1[ind] = ry[i + 6] * gF1Dr_g[ind] + sy[i + 6] * gF1Ds_g[ind];
      }
    }
  }

  if(reverse[2]) {
    for(int j = 0; j < 10; j++) {
      for(int i = 0; i < 6; i++) {
        int ind = j * 6 + i;
        Dx2[ind] = rx[i + 2 * 6] * gF2DrR_g[ind] + sx[i + 2 * 6] * gF2DsR_g[ind];
        Dy2[ind] = ry[i + 2 * 6] * gF2DrR_g[ind] + sy[i + 2 * 6] * gF2DsR_g[ind];
      }
    }
  } else {
    for(int j = 0; j < 10; j++) {
      for(int i = 0; i < 6; i++) {
        int ind = j * 6 + i;
        Dx2[ind] = rx[i + 2 * 6] * gF2Dr_g[ind] + sx[i + 2 * 6] * gF2Ds_g[ind];
        Dy2[ind] = ry[i + 2 * 6] * gF2Dr_g[ind] + sy[i + 2 * 6] * gF2Ds_g[ind];
      }
    }
  }
}

// host stub function
void op_par_loop_init_gauss_grad_neighbour(char const *name, op_set set,
  op_arg arg0,
  op_arg arg1,
  op_arg arg2,
  op_arg arg3,
  op_arg arg4,
  op_arg arg5,
  op_arg arg6,
  op_arg arg7,
  op_arg arg8,
  op_arg arg9,
  op_arg arg10){

  int nargs = 11;
  op_arg args[11];

  args[0] = arg0;
  args[1] = arg1;
  args[2] = arg2;
  args[3] = arg3;
  args[4] = arg4;
  args[5] = arg5;
  args[6] = arg6;
  args[7] = arg7;
  args[8] = arg8;
  args[9] = arg9;
  args[10] = arg10;

  // initialise timers
  double cpu_t1, cpu_t2, wall_t1, wall_t2;
  op_timing_realloc(6);
  op_timers_core(&cpu_t1, &wall_t1);
  OP_kernels[6].name      = name;
  OP_kernels[6].count    += 1;


  if (OP_diags>2) {
    printf(" kernel routine w/o indirection:  init_gauss_grad_neighbour");
  }

  int set_size = op_mpi_halo_exchanges_cuda(set, nargs, args);


  if (set_size >0) {


    //Set up typed device pointers for OpenACC

    int* data0 = (int*)arg0.data_d;
    double* data1 = (double*)arg1.data_d;
    double* data2 = (double*)arg2.data_d;
    double* data3 = (double*)arg3.data_d;
    double* data4 = (double*)arg4.data_d;
    double* data5 = (double*)arg5.data_d;
    double* data6 = (double*)arg6.data_d;
    double* data7 = (double*)arg7.data_d;
    double* data8 = (double*)arg8.data_d;
    double* data9 = (double*)arg9.data_d;
    double* data10 = (double*)arg10.data_d;
    #pragma acc parallel loop independent deviceptr(data0,data1,data2,data3,data4,data5,data6,data7,data8,data9,data10)
    for ( int n=0; n<set->size; n++ ){
      init_gauss_grad_neighbour_openacc(
        &data0[3*n],
        &data1[18*n],
        &data2[18*n],
        &data3[18*n],
        &data4[18*n],
        &data5[60*n],
        &data6[60*n],
        &data7[60*n],
        &data8[60*n],
        &data9[60*n],
        &data10[60*n]);
    }
  }

  // combine reduction data
  op_mpi_set_dirtybit_cuda(nargs, args);

  // update kernel record
  op_timers_core(&cpu_t2, &wall_t2);
  OP_kernels[6].time     += wall_t2 - wall_t1;
  OP_kernels[6].transfer += (float)set->size * arg0.size;
  OP_kernels[6].transfer += (float)set->size * arg1.size * 2.0f;
  OP_kernels[6].transfer += (float)set->size * arg2.size * 2.0f;
  OP_kernels[6].transfer += (float)set->size * arg3.size * 2.0f;
  OP_kernels[6].transfer += (float)set->size * arg4.size * 2.0f;
  OP_kernels[6].transfer += (float)set->size * arg5.size * 2.0f;
  OP_kernels[6].transfer += (float)set->size * arg6.size * 2.0f;
  OP_kernels[6].transfer += (float)set->size * arg7.size * 2.0f;
  OP_kernels[6].transfer += (float)set->size * arg8.size * 2.0f;
  OP_kernels[6].transfer += (float)set->size * arg9.size * 2.0f;
  OP_kernels[6].transfer += (float)set->size * arg10.size * 2.0f;
}
