//
// auto-generated by op2.py
//

void ls_add_diff_omp4_kernel(
  double *data0,
  int dat0size,
  double *data1,
  int dat1size,
  double *data2,
  int dat2size,
  double *data3,
  int dat3size,
  double *data4,
  int dat4size,
  double *data5,
  int dat5size,
  int count,
  int num_teams,
  int nthread){

  #pragma omp target teams num_teams(num_teams) thread_limit(nthread) map(to:data0[0:dat0size],data1[0:dat1size],data2[0:dat2size],data3[0:dat3size],data4[0:dat4size],data5[0:dat5size])
  #pragma omp distribute parallel for schedule(static,1)
  for ( int n_op=0; n_op<count; n_op++ ){
    //variable mapping
    const double *diff = &data0[3*n_op];
    double *rk = &data1[3*n_op];
    double *dsldx = &data2[9*n_op];
    double *dsrdx = &data3[9*n_op];
    double *dsldy = &data4[9*n_op];
    double *dsrdy = &data5[9*n_op];

    //inline function
    
    for(int i = 0; i < 3; i++) {
      rk[i] = rk[i] + diff[i];
    }

    for(int i = 0; i < 9; i++) {
      dsldx[i] = 0.0;
      dsrdx[i] = 0.0;
      dsldy[i] = 0.0;
      dsrdy[i] = 0.0;
    }
    //end inline func
  }

}
