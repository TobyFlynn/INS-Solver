//
// auto-generated by op2.py
//

void glb_ind_kernelBC_omp4_kernel(
  int *map0,
  int map0size,
  int *data1,
  int dat1size,
  int *data0,
  int dat0size,
  int *col_reord,
  int set_size1,
  int start,
  int end,
  int num_teams,
  int nthread){

  #pragma omp target teams num_teams(num_teams) thread_limit(nthread) map(to:data1[0:dat1size])\
    map(to:col_reord[0:set_size1],map0[0:map0size],data0[0:dat0size])
  #pragma omp distribute parallel for schedule(static,1)
  for ( int e=start; e<end; e++ ){
    int n_op = col_reord[e];
    int map0idx;
    map0idx = map0[n_op + set_size1 * 0];

    //variable mapping
    const int *glb = &data0[1 * map0idx];
    int *glbBC = &data1[1*n_op];

    //inline function
    
    glbBC[0] = glb[0];
    //end inline func
  }

}
