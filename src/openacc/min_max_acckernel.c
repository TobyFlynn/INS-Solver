//
// auto-generated by op2.py
//

//user function
//user function
//#pragma acc routine
inline void min_max_openacc( double *min, double *max, const double *arr) {
  for(int i = 0; i < 15; i++) {
    if(arr[i] > *max) {
      *max = arr[i];
    }
    if(arr[i] < *min) {
      *min = arr[i];
    }
  }
}

// host stub function
void op_par_loop_min_max(char const *name, op_set set,
  op_arg arg0,
  op_arg arg1,
  op_arg arg2){

  double*arg0h = (double *)arg0.data;
  double*arg1h = (double *)arg1.data;
  int nargs = 3;
  op_arg args[3];

  args[0] = arg0;
  args[1] = arg1;
  args[2] = arg2;

  // initialise timers
  double cpu_t1, cpu_t2, wall_t1, wall_t2;
  op_timing_realloc(13);
  op_timers_core(&cpu_t1, &wall_t1);
  OP_kernels[13].name      = name;
  OP_kernels[13].count    += 1;


  if (OP_diags>2) {
    printf(" kernel routine w/o indirection:  min_max");
  }

  int set_size = op_mpi_halo_exchanges_cuda(set, nargs, args);

  double arg0_l = arg0h[0];
  double arg1_l = arg1h[0];

  if (set_size >0) {


    //Set up typed device pointers for OpenACC

    double* data2 = (double*)arg2.data_d;
    #pragma acc parallel loop independent deviceptr(data2) reduction(min:arg0_l) reduction(max:arg1_l)
    for ( int n=0; n<set->size; n++ ){
      min_max_openacc(
        &arg0_l,
        &arg1_l,
        &data2[15*n]);
    }
  }

  // combine reduction data
  arg0h[0]  = MIN(arg0h[0],arg0_l);
  op_mpi_reduce_double(&arg0,arg0h);
  arg1h[0]  = MAX(arg1h[0],arg1_l);
  op_mpi_reduce_double(&arg1,arg1h);
  op_mpi_set_dirtybit_cuda(nargs, args);

  // update kernel record
  op_timers_core(&cpu_t2, &wall_t2);
  OP_kernels[13].time     += wall_t2 - wall_t1;
  OP_kernels[13].transfer += (float)set->size * arg2.size;
}
