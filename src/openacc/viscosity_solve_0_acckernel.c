//
// auto-generated by op2.py
//

//user function
//user function
//#pragma acc routine
inline void viscosity_solve_0_openacc( const double *J, const double *Dx,
                              const double *Dy, const double *mu,
                              const double *rho, const double *factor,
                              const double *mm, const double *u, double *rhs) {
  double tmpX[46*15];
  double tmpY[46*15];

  for(int m = 0; m < 46; m++) {
    for(int n = 0; n < 15; n++) {
      int ind = m * 15 + n;
      tmpX[ind] = J[m] * cubW_g[m] * Dx[ind] * mu[m];
      tmpY[ind] = J[m] * cubW_g[m] * Dy[ind] * mu[m];
    }
  }

  double op[15*15];
  for(int i = 0; i < 15; i++) {
    for(int j = 0; j < 15; j++) {
      int c_ind = i * 15 + j;
      op[c_ind] = 0.0;
      for(int k = 0; k < 46; k++) {

        int b_ind = k * 15 + j;

        int a_ind = k * 15 + i;
        op[c_ind] += Dx[a_ind] * tmpX[b_ind] + Dy[a_ind] * tmpY[b_ind];
      }
    }
  }

  for(int i = 0; i < 15; i++) {
    rhs[i] = 0.0;
    for(int j = 0; j < 15; j++) {
      int op_ind = i * 15 + j;
      rhs[i] += op[op_ind] * u[j];
      rhs[i] += mm[op_ind] * u[j] * rho[j] * (*factor);
    }
  }
}

// host stub function
void op_par_loop_viscosity_solve_0(char const *name, op_set set,
  op_arg arg0,
  op_arg arg1,
  op_arg arg2,
  op_arg arg3,
  op_arg arg4,
  op_arg arg5,
  op_arg arg6,
  op_arg arg7,
  op_arg arg8){

  double*arg5h = (double *)arg5.data;
  int nargs = 9;
  op_arg args[9];

  args[0] = arg0;
  args[1] = arg1;
  args[2] = arg2;
  args[3] = arg3;
  args[4] = arg4;
  args[5] = arg5;
  args[6] = arg6;
  args[7] = arg7;
  args[8] = arg8;

  // initialise timers
  double cpu_t1, cpu_t2, wall_t1, wall_t2;
  op_timing_realloc(31);
  op_timers_core(&cpu_t1, &wall_t1);
  OP_kernels[31].name      = name;
  OP_kernels[31].count    += 1;


  if (OP_diags>2) {
    printf(" kernel routine w/o indirection:  viscosity_solve_0");
  }

  int set_size = op_mpi_halo_exchanges_cuda(set, nargs, args);

  double arg5_l = arg5h[0];

  if (set_size >0) {


    //Set up typed device pointers for OpenACC

    double* data0 = (double*)arg0.data_d;
    double* data1 = (double*)arg1.data_d;
    double* data2 = (double*)arg2.data_d;
    double* data3 = (double*)arg3.data_d;
    double* data4 = (double*)arg4.data_d;
    double* data6 = (double*)arg6.data_d;
    double* data7 = (double*)arg7.data_d;
    double* data8 = (double*)arg8.data_d;
    #pragma acc parallel loop independent deviceptr(data0,data1,data2,data3,data4,data6,data7,data8)
    for ( int n=0; n<set->size; n++ ){
      viscosity_solve_0_openacc(
        &data0[46*n],
        &data1[690*n],
        &data2[690*n],
        &data3[46*n],
        &data4[46*n],
        &arg5_l,
        &data6[225*n],
        &data7[15*n],
        &data8[15*n]);
    }
  }

  // combine reduction data
  op_mpi_set_dirtybit_cuda(nargs, args);

  // update kernel record
  op_timers_core(&cpu_t2, &wall_t2);
  OP_kernels[31].time     += wall_t2 - wall_t1;
  OP_kernels[31].transfer += (float)set->size * arg0.size;
  OP_kernels[31].transfer += (float)set->size * arg1.size;
  OP_kernels[31].transfer += (float)set->size * arg2.size;
  OP_kernels[31].transfer += (float)set->size * arg3.size;
  OP_kernels[31].transfer += (float)set->size * arg4.size;
  OP_kernels[31].transfer += (float)set->size * arg6.size;
  OP_kernels[31].transfer += (float)set->size * arg7.size;
  OP_kernels[31].transfer += (float)set->size * arg8.size * 2.0f;
}
